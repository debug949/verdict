# GitHub App
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=        # base64-encoded PEM: base64 -i key.pem | tr -d '\n'
GITHUB_WEBHOOK_SECRET=         # node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
